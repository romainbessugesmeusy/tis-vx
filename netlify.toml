[build]
  base = "viewer"
  command = "npm install && npm run build"
  publish = "dist"

# SPA deep linking - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
